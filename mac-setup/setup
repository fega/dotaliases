sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"


# show app switcherr in all screens
defaults write com.apple.Dock appswitcher-all-displays -bool true
killall Dock
# Install diff tool
# https://github.com/bigH/git-fuzzy

brew install fzf git-delta exa
git clone https://github.com/bigH/git-fuzzy.git
echo "export PATH=\"$(pwd)/git-fuzzy/bin:\$PATH\"" >> ~/.zshrc

# googler search tool
  # duck duck go search tool
brew install ddgr 
  # email client and tools
brew install  neomutt w3m mailcap 
  # a replacemement for postman
bre install httpie jq
  # command correction
bre install thefuck 
  # node version manager
bre install nvm yarn 
  # programming languages
bre install go rust
  # to download youtube videos
bre install youtube-dl 
  # a better cat
bre install bat 
  # alternative to grep
bre install ack 
  # file converter
bre install pandoc 
  # ocr utility
bre install tesseract 
  # tunnelblick ctl
bre install tunnelblickctl 
  # for testing cloudformation templates
bre install cfn-lint 
  # other well known utilities
git awscli vim curl docker htop lynx ffmpeg aws-sam-cli

brew tap browsh-org/homebrew-browsh
brew install browsh
brew install --cask mysqlworkbench
brew install --cask  slack
brew install --cask  notion
brew install --cask  tunnelblick
brew install --cask  visual-studio-code
brew install --cask  postman
brew install --cask  iterm2
brew install --cask  telegram
brew install --cask  whatsapp
brew install --cask  spotify
brew install --cask  kindle
brew install --cask  google-chrome
brew install --cask  notunes
brew install --cask  dynamodb-local
brew install --cask  dynamodb-local
brew install  amazon-ecs-cli 
npm i -g moro 

gem install terjira
gem install timetrap

# add brew dynamodb-local
# add font-fira-code iterm2 postman robo-3t visual-studio-code
mas install 441258766
